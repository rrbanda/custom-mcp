apiVersion: v1
kind: Service
metadata:
  name: cmp-mcp-server
  labels:
    app: cmp-mcp-server
spec:
  selector:
    app: cmp-mcp-server
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
